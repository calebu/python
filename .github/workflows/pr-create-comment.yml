name: auto-comment-on-pr
on: [pull_request]
jobs: 
  auto-comment:
    runs-on: ubuntu-latest
    steps:
    - name: Check PR Changes
      id: pr-changes
      run: echo "::set-output name=changes::${{ toJson(github.event.pull_request.changed_files) }}"

    - uses: actions/checkout@v2
      name: Checkout current commit
      with:
        ref: main
        path: main
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11' # Specify your desired Python version
        cache: 'pip' # Optional: Caches dependencies for faster subsequent runs
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests # Replace 'requests' with your library name
    - name: Create Comment
      if: steps.pr-changes.outputs.changes != '[]'
      run: |
        # Use GitHub API to create a comment on the PR
        PR_NUMBER=${{ github.event.pull_request.number }}
        COMMENT="$(python3 ./main/sample.py check-changes)"
        echo "The comment body is: $COMMENT"
        GITHUB_TOKEN=${{ secrets.GHA_PAT }}
        COMMENT_URL="https://api.github.com/repos/${{ github.repository }}/issues/${PR_NUMBER}/comments"

        curl -s -H "Authorization: token ${GITHUB_TOKEN}" -X POST $COMMENT_URL -d "{\"body\":\"$COMMENT\"}"
      
